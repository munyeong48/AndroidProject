<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sjhapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="AnalyticsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config" >

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <data android:scheme="gadata"/>  <!--페이지에 대한 딥링크를 어떤 url로 사용할 것인지 지정-->
                <action android:name="android.intent.action.VIEW"/>  <!--액션은 intent에 첨부되는 data의 url이 가리키는 데이터를 사용자에게 보여줌-->
                <category android:name="android.intent.category.DEFAULT"/>  <!--카테고리 선언 -> 앱 이름을 지정하지 않고 딥링크 사용가능-->
                <category android:name="android.intent.category.BROWSABLE"/>  <!--카테고리 선언 -> 웹브라우저에서 링크를 열 수 있게 함-->
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"></activity>
        <activity android:name=".WebViewActivity"></activity>
        <activity android:name=".NextActivity"></activity>
    </application>

</manifest>